{
  "name": "communityhub-lambda-notifications",
  "version": "1.0.0",
  "private": true,
  "description": "Funciones Lambda de CommunityHub para notificaciones de capacidad y recordatorios de actividades.",
  "main": "src/handler.js",
  "scripts": {
    "check": "node --check src/handler.js && node --check src/reminderHandler.js && node --check src/database.js"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  }
}
